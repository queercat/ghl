# ghl

this Rust program allows you to create a new Git repository and initialize it on GitHub using the GitHub CLI.

## prerequisites

- git must be installed on your system.
- gitHub CLI (gh) must be installed and configured.

## usage

```sh
ghl 
ghl --public
ghl --private
ghl --name {NAME_OF_REPOSITORY}
```